import { RecaptchaDirective } from './recaptcha.directive';

xdescribe('RecaptchaDirective', () => {
    xit('should create an instance', () => {
        // const directive = new RecaptchaDirective();
        // expect(directive).toBeTruthy();
    });
});
